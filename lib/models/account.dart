// Balance Account model
class Account {
  final firstName;
  final lastName;
  final password;
  final pin;
  final accountBalance;
  final email;

  Account({this.firstName, this.lastName, this.password, this.pin, this.accountBalance, this.email});
}
